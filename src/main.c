/*
 Main CLI for PiDB
 */
#include <stdio.h>
#include <piMath.h>

int main() {
	printf("Hello World %d.%d%d%d%d%d\n", piDigit(0), piDigit(1), piDigit(2), piDigit(3), piDigit(4), piDigit(5));
	return 0;
}
